Fast file pattern matching tool that works with any codebase size.

- Supports glob patterns like "**/*.js" or "src/**/*.ts".
- Returns matching file paths sorted by modification time; max {{ .MaxResults }} results; skips hidden files.
- Use this tool when you need to find files by name patterns. Use grep to search file contents.
