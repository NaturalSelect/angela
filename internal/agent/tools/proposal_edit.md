Revise the branch's proposal document by exact find-and-replace, without writing it out again. This is the normal way to change a proposal after the first draft: send only the passage that changes, not the whole text. `old_string` must match exactly once, so include enough surrounding context to be unique — otherwise the edit is refused and the document is left untouched; set `replace_all` to change every occurrence instead. If `old_string` differs from the document only in whitespace the matching lines are still edited and `new_string` is re-indented, and the reply says so. Draft the document with `proposal_write` before editing it.
