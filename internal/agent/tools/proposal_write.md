Write the branch's proposal document, replacing whatever it held before. The proposal is held in memory for this branch only — it is not a file, it touches nothing in the working tree, and no permission prompt stands in front of it. Use this for the first draft and for a rewrite so extensive that patching would be pointless; for anything narrower use `ProposalEdit`, which costs a fraction of the output. The reply confirms the save without echoing the text back; call `ProposalRead` if you need to see it again. The document is what `Merge` hands back to the suspended conversation, so write it for the agent receiving it.
